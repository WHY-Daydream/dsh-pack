# v0.5.0 — Post-Release Verification Record (PVR)

**Date**: 2026-08-31
**Release**: `@why-daydream/dsh-pack@0.5.0` — Verifiable Agent Artifact
**Release commit**: `09f7f27d8d0900f8774dad1aa748cdc896397de7`（release/v0.5.0）
**Verification scope**: 最小 Post-Release Verification（无新增测试/功能，只确认发布本体与卫生状态）

## PVR 结果

| # | Check | 期望 | 结果 |
|---|-------|------|------|
| PVR-01 | npm dist-tag `latest` | `0.5.0` | ✅ PASS（`npm view dist-tags.latest` = 0.5.0） |
| PVR-02 | npm registry clean-room install | install + import OK | ✅ PASS（全新目录 `npm install @0.5.0`，0 漏洞，import 模块图加载成功） |
| PVR-03 | published integrity == audited tarball | 同 sha512 | ✅ PASS（`npm view dist.integrity` 与 publish notice / 审计 tgz 一致） |
| PVR-04 | GitHub tag `v0.5.0` → commit | `09f7f27` | ✅ PASS（annotated tag 对象 → commit `09f7f27d8…`） |
| PVR-05 | GitHub Release 状态 | non-draft / non-prerelease | ✅ PASS（`draft: false, prerelease: false`） |
| PVR-06 | release commit 在 main 中 | ancestor of `origin/main` | ✅ PASS（release-only PR #14 已合并 `ee18312`，`09f7f27` 已是 main ancestor） |
| PVR-07 | 泄露的 npm token 已吊销 | 服务端 revoke | ✅ PASS（用户已在 npmjs Access Tokens 页面 revoke，本地零残留） |
| PVR-08 | working tree / credentials 清洁 | 无 token 残留 | ✅ PASS（userconfig `/root/.npmrc` 无 `_authToken`、无项目 .npmrc、全局 `_authToken` 空、git 树 clean） |

## Release 冻结状态

```
v0.5.0 — Verifiable Agent Artifact

Git tag                   ✅ v0.5.0 @ 09f7f27
GitHub Release            ✅ https://github.com/WHY-Daydream/dsh-pack/releases/tag/v0.5.0
npm package               ✅ @why-daydream/dsh-pack@0.5.0 (latest)
typecheck                 ✅
vitest 324/324            ✅
7 adversarial surfaces    ✅ N3/N7/N4/N5/N1/N2/N6
5 real flaws fixed        ✅（全部 minimal fix）
16 release invariants     ✅ RI-01–RI-16
RG-01–RG-08               ✅
registry clean-room       ✅
```

## Identity domains（发布时保持不混淆）

```
configHash        ≠  contentHash
contentHash       ≠  OCI blobDigest / OCI manifestDigest
Evidence statement/document digest
npm integrity
Git commit
```

OCIm 与 DSH 两个身份域各自独立：`OCI manifestDigest ≠ DSH contentHash` 永远成立。

## 关闭 v0.5 后的路线（roadmap，不在本次实现）

- **v0.6 — Distributed Verifiable Evidence**（首选）：OCI Referrers / subject-based Evidence discovery，
  provenance/SBOM/capability/runtime-attestation 跟随 Agent Image 远程分发；已冻结原则 **D140–D148**
  （OCI attachment identity ≠ Evidence subject identity；Registry 只提供 discovery/transport，永远不是
  trust authority；remote Evidence 全链验证；registry order 不选 winner；missing ≠ absence；
  evidence 可缓存、trust verdicts 不可复用；分发不改变 contentHash；拉取不执行代码）。
- **v0.7 — Isolated Agent Runtime**（Strong Runtime Isolation，SandboxProvider 抽象）。
- **v0.8 — Confidential Agent Artifact / Encryption**（后置）。
