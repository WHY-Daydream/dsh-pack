# v0.6.0 — RG6-10 Release Artifact Consistency 记录

**Date**: 2026-08-31
**Baseline**: `main`（ORAS gate PASS 后将创建 release branch / tag）
**Status**: 准备完成（exact artifact 指纹已记录）；**tag / GitHub Release / npm publish 被 RG6-09（real ORAS bidirectional smoke）门控**——ORAS PASS 前禁止执行。

## Exact audited artifact（RG6-04/RG6-05 审计产物）

`npm pack` 产物（本地审计 + clean-room 安装验证通过）：

| 字段 | 值 |
|---|---|
| filename | `why-daydream-dsh-pack-0.6.0.tgz` |
| package name | `@why-daydream/dsh-pack` |
| version | `0.6.0` |
| package size | 159.8 kB（159828 bytes） |
| unpacked size | 601.6 kB（601642 bytes） |
| total files | 103 |
| **shasum** | `496bcb7a023507e3cdded0f0e6bfbbbd5e6c9417` |
| **integrity** | `sha512-iJhUm9gkJxcq/[…]fzk82sSEHfmyA==`（npm 完整值在 pack 时记录） |
| files 白名单 | `lib` / `cordis.patch.yml` / `README.md` / `LICENSE`（+ README.zh-CN.md 自动） |
| 边界审计 | 103 files 零异常（无 .map/.pem/.key/spec/test/scripts/.github/docs 泄漏，RG6-04） |
| clean-room | `/tmp/dsh-cleanroom-0.6.0` 安装 + import 模块图（trust/discovery/publication/cache/client/policy）全加载（RG6-05） |
| secret scan | pack tarball 解包扫描 `sk-`/`ghp_`/`npm_`/`BEGIN PRIVATE KEY`/`_authToken` → **NONE**（RG6-08） |

**发布时铁律**：publish 到 npm 的 tgz 必须是上述 exact artifact——`npm pack` 后比对 `shasum`/`integrity` 一致才允许上传；任何不一致（包括重新 pack 产生的不同字节）必须重新走 RG6-04/05/08 审计。

## RG6-10 执行 checklist（ORAS PASS 后）

```text
[ ] 1. 确认 RG6-09（real ORAS 双向 smoke）PASS —— 唯一前置
[ ] 2. 从 main 创建 release branch（release/v0.6.0）
[ ] 3. clean frozen install + typecheck + full vitest 复跑（release branch 上）
[ ] 4. npm pack → 比对 shasum 496bcb7a023507e3cdded0f0e6bfbbbd5e6c9417（exact artifact）
[ ] 5. annotated tag `v0.6.0`（release branch HEAD，tag message 引用 CHANGELOG 0.6.0）
[ ] 6. GitHub Release（title: v0.6.0 — Distributed Verifiable Evidence；attach exact tgz）
[ ] 7. npm publish 该 exact tgz（非重新 pack）
[ ] 8. registry clean-room install 复核（全新目录 npm install @why-daydream/dsh-pack@0.6.0 + import 验证）
[ ] 9. 收口：确认 GitHub Release + npm 上的 version/artifact 与本地审计产物一致
```

## 一致性对照表（发布后回填）

| 位置 | version | shasum | 状态 |
|------|---------|--------|------|
| 本地审计 tgz | 0.6.0 | 496bcb7a… | ✅ 已记录 |
| GitHub Release asset | | | ⏳ ORAS PASS 后 |
| npm registry | | | ⏳ ORAS PASS 后 |
